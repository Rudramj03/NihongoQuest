# ==========================================================================
# NihongoQuest — nginx vhost
#
#   source   : /etc/nginx/sites-available/nihongoquest.conf
#   enabled  : /etc/nginx/sites-enabled/nihongoquest.conf -> sites-available/nihongoquest.conf
#   domain   : https://nihongo.13-61-25-153.sslip.io
#   upstream : 127.0.0.1:5001 (pm2: nihongoquest — gunicorn, 2 workers)
#
# Snapshot taken 2026-07-31. This is a reference copy — nginx never reads it.
# Edit the source file above, then: sudo nginx -t && sudo systemctl reload nginx
# ==========================================================================

server {
    server_name nihongo.13-61-25-153.sslip.io;

    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen [::]:443 ssl; # managed by Certbot
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/nihongo.13-61-25-153.sslip.io/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/nihongo.13-61-25-153.sslip.io/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}
server {
    if ($host = nihongo.13-61-25-153.sslip.io) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    listen [::]:80;
    server_name nihongo.13-61-25-153.sslip.io;
    return 404; # managed by Certbot


}